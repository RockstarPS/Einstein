This example can be built with
    make PROJECT=INTEGRITY clean all 
    


Note:
- You need to adjust the include folders in makefile.
- There are other projects support (cmp. makefile and use PROJECT=<...> ).

 